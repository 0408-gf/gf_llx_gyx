# 胜算 · 足球比赛预测

一个无需后端、可直接在浏览器运行的足球赛果预测应用。用户可以调整两队的进攻、防守、近期状态与主场优势，应用通过泊松分布计算主胜、平局、客胜概率以及最可能比分。

## 本地运行

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 模型说明

应用先将球队的三项能力指标换算为双方预期进球数（xG），再遍历双方 0–10 球的比分组合，并使用泊松概率质量函数汇总赛果概率。结果仅用于数据分析与娱乐，不构成投注建议。
